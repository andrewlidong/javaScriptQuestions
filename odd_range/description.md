# Odd Range

Write a function oddRange(end) that takes in a number and returns an array containing all positive odd numbers up to the int, end.  

Examples:

```js
oddRange(13); // => [ 1, 3, 5, 7, 9, 11, 13 ]
oddRange(6); // => [ 1, 3, 5 ]
```