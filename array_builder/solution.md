# Array Builder

```js
const arrayBuilder = obj => {
    const result = [];

    for (let ke in obj) {
        let end = obj[key];

        for (let i = 1; i <= end; i++) {
            result.push(key);
        }
    }

    return result;
}
```